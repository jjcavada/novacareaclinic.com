---
phase: 02-core-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/legal-compliance-hub/page.tsx
  - src/app/homepage/page.tsx
  - src/app/about/page.tsx
  - src/app/services/page.tsx
  - src/app/providers/page.tsx
  - src/app/patient-resources/page.tsx
  - src/app/patient-portal/page.tsx
  - src/app/contact/page.tsx
  - src/app/insurance-and-payment/page.tsx
  - src/app/schedule-appointment/page.tsx
autonomous: true
---

<objective>
Implement comprehensive metadata and canonical URLs across all pages.

Purpose: Establish foundational SEO with proper meta tags, keywords, and canonical URLs for search engine discoverability.
Output: All 11 pages with standardized metadata including title, description, keywords, and canonical URLs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md

@src/app/layout.tsx
@src/app/homepage/page.tsx
@src/app/legal-compliance-hub/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add metadataBase and enhance root layout metadata</name>
  <files>src/app/layout.tsx</files>
  <action>
Update the root layout.tsx metadata to include:

1. Add `metadataBase: new URL('https://novacareclinic.com')` - this enables canonical URL generation site-wide
2. Enhance the default metadata object to include:
   - `keywords: ['behavioral health clinic', 'mental health services', 'Phoenix psychiatrist', 'anxiety therapy', 'depression treatment', 'psychiatric care Phoenix AZ']`
   - `authors: [{ name: 'NovaCare Clinic' }]`
   - `creator: 'NovaCare Clinic'`
   - `publisher: 'NovaCare Clinic'`
3. Update title to use template pattern: `title: { default: 'NovaCare Clinic - Compassionate Behavioral Health Care', template: '%s | NovaCare Clinic' }`
4. Keep existing viewport and icons configuration

Do NOT add Open Graph or Twitter Cards yet (that's Phase 3).
  </action>
  <verify>npm run type-check passes and npm run build succeeds</verify>
  <done>layout.tsx has metadataBase, keywords, and title template configured</done>
</task>

<task type="auto">
  <name>Task 2: Fix legal-compliance-hub page metadata</name>
  <files>src/app/legal-compliance-hub/page.tsx</files>
  <action>
The legal-compliance-hub page is currently a 'use client' component with no metadata export. Fix this by:

1. Create a new server component wrapper that exports metadata:
   ```typescript
   import type { Metadata } from 'next';
   import LegalComplianceHub from './components/LegalComplianceHub';

   export const metadata: Metadata = {
     title: 'Legal & Compliance',
     description: 'NovaCare Clinic legal documentation including Anti-Kickback Compliance Policy, Privacy Policy, and HIPAA Notice of Privacy Practices. Healthcare law compliance information.',
     keywords: ['HIPAA', 'privacy policy', 'healthcare compliance', 'anti-kickback', 'patient rights', 'NovaCare Clinic legal'],
   };
   ```

2. Move the existing client component to `src/app/legal-compliance-hub/components/LegalComplianceHub.tsx`

3. The new page.tsx should import and render the client component

4. Add Header component import and render it before the LegalComplianceHub component (consistent with other pages)
  </action>
  <verify>npm run build succeeds and legal-compliance-hub page renders correctly at localhost:4028/legal-compliance-hub</verify>
  <done>legal-compliance-hub has metadata export and follows page/component pattern like other pages</done>
</task>

<task type="auto">
  <name>Task 3: Standardize metadata across all pages</name>
  <files>
    src/app/homepage/page.tsx,
    src/app/about/page.tsx,
    src/app/services/page.tsx,
    src/app/providers/page.tsx,
    src/app/patient-resources/page.tsx,
    src/app/patient-portal/page.tsx,
    src/app/contact/page.tsx,
    src/app/insurance-and-payment/page.tsx,
    src/app/schedule-appointment/page.tsx
  </files>
  <action>
Update metadata on each page to:

1. Use simplified title (template from layout.tsx will add "| NovaCare Clinic"):
   - homepage: 'Compassionate Behavioral Health Care in Phoenix'
   - about: 'About Us - Our Mission & Team'
   - services: 'Mental Health Services'
   - providers: 'Our Healthcare Providers'
   - patient-resources: 'Mental Health Resources'
   - patient-portal: 'Patient Portal'
   - contact: 'Contact Us'
   - insurance-and-payment: 'Insurance & Payment Options'
   - schedule-appointment: 'Schedule an Appointment'

2. Add keywords array specific to each page:
   - homepage: ['behavioral health Phoenix', 'mental health clinic Arizona', 'psychiatrist Phoenix', 'therapy services', 'crisis support 24/7']
   - about: ['NovaCare Clinic team', 'behavioral health mission', 'Phoenix mental health providers', 'community healthcare']
   - services: ['psychiatric evaluation', 'individual therapy', 'group therapy', 'medication management', 'telehealth therapy', 'crisis intervention']
   - providers: ['psychiatrist Phoenix', 'licensed therapist', 'behavioral health professional', 'mental health team']
   - patient-resources: ['mental health resources', 'depression information', 'anxiety resources', 'ADHD information', 'PTSD resources']
   - patient-portal: ['patient portal', 'medical records', 'telehealth', 'appointment scheduling']
   - contact: ['contact NovaCare', 'Phoenix clinic address', 'mental health appointment', 'crisis support']
   - insurance-and-payment: ['insurance accepted', 'sliding fee scale', 'mental health payment', 'financial assistance']
   - schedule-appointment: ['book appointment', 'schedule therapy', 'new patient appointment', 'psychiatry appointment']

3. Add alternates for canonical URL (Next.js will use metadataBase):
   ```typescript
   alternates: {
     canonical: '/page-path',
   }
   ```

Keep existing descriptions - they are already well-written.
  </action>
  <verify>npm run build succeeds; inspect page source at each route to confirm metadata renders correctly</verify>
  <done>All 9 pages have standardized titles, keywords, and canonical URLs</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes
- [ ] `npm run build` succeeds without errors
- [ ] View source on homepage shows meta keywords tag
- [ ] View source shows canonical URL
- [ ] legal-compliance-hub page loads with Header and metadata
</verification>

<success_criteria>
- All tasks completed
- All 11 pages have consistent metadata pattern
- Keywords meta tag present on all pages
- Canonical URLs configured via metadataBase
- Title template working (pages show "Title | NovaCare Clinic")
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-seo/02-01-SUMMARY.md`
</output>
